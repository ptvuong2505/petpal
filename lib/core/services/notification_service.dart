class NotificationService {
  Future<void> scheduleReminder({
    required String title,
    required DateTime reminderTime,
  }) async {
    // Local notification setup can be added later by Huy's reminder task.
  }
}
